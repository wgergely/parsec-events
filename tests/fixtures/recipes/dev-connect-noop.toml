name = "dev-connect-noop"
description = "Dev test recipe for watcher validation. Captures a snapshot only — no display changes."
event_type = "connect"

[[steps]]
id = "capture-test-snapshot"
ingredient = "display.snapshot"
operation = "capture"
depends_on = []
verify = false
compensation_policy = "none"

[steps.arguments]
snapshot_name = "watcher-live-test"
