name = "condition-skips"
description = "Thin recipe runner fixture that skips a step when the event_type condition does not match."
event_type = "disconnect"

[[steps]]
id = "skip-on-event-type-mismatch"
ingredient = "set-resolution"
operation = "apply"
depends_on = []
verify = true
compensation_policy = "explicit"
retry_count = 0
retry_delay_ms = 0
allow_diagnostics = false

[steps.condition]
event_type_is = "connect"

[steps.arguments]
width = 1280
height = 720
